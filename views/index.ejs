<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Microsoft JhengHei', sans-serif; background: linear-gradient(135deg, #E74C3C, #C0392B); min-height: 100vh; display: flex; align-items: center; justify-content: center; }
        .container { text-align: center; color: white; padding: 40px; }
        h1 { font-size: 3rem; margin-bottom: 20px; }
        .emoji { font-size: 5rem; margin-bottom: 20px; }
        p { font-size: 1.2rem; margin-bottom: 30px; opacity: 0.9; }
        .stats { display: flex; justify-content: center; gap: 40px; margin: 30px 0; }
        .stat { background: rgba(255,255,255,0.2); padding: 20px 30px; border-radius: 10px; }
        .stat-number { font-size: 2rem; font-weight: bold; }
        .stat-label { font-size: 0.9rem; opacity: 0.8; }
        .btn { display: inline-block; background: white; color: #E74C3C; padding: 15px 40px; border-radius: 30px; text-decoration: none; font-weight: bold; font-size: 1.1rem; margin-top: 20px; transition: transform 0.3s; }
        .btn:hover { transform: scale(1.05); }
    </style>
</head>
<body>
    <div class="container">
        <div class="emoji">🌅</div>
        <h1>退休福音</h1>
        <p>智慧生活規劃助手<br>讓 AI 幫您規劃精彩的退休生活</p>
        <div class="stats">
            <div class="stat">
                <div class="stat-number"><%= activityCount %></div>
                <div class="stat-label">精選活動</div>
            </div>
            <div class="stat">
                <div class="stat-number"><%= userCount %></div>
                <div class="stat-label">活躍用戶</div>
            </div>
        </div>
        <a href="https://line.me/R/ti/p/@024wclps" class="btn">📱 加入 LINE 好友</a>
    </div>
</body>
</html>