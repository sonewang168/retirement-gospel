<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>退休福音 - 管理後台</title>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- PWA 設定 -->
    <link rel="manifest" href="/manifest.json">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="退休福音">
    <meta name="theme-color" content="#E74C3C">
    <link rel="apple-touch-icon" href="https://fakeimg.pl/192x192/E74C3C/fff/?text=🌅&font_size=100">
    
    <style>
        body { 
            -webkit-tap-highlight-color: transparent;
            padding-top: env(safe-area-inset-top);
            padding-bottom: env(safe-area-inset-bottom);
        }
        * { outline: none; }
        @media (display-mode: standalone) {
            .pwa-hint { display: none !important; }
        }
        .stats-card {
            cursor: pointer;
            transition: all 0.2s;
        }
        .stats-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        .stats-card:active {
            transform: scale(0.98);
        }
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }
        .modal.active {
            display: flex;
        }
        .modal-content {
            background: white;
            border-radius: 16px;
            width: 90%;
            max-width: 600px;
            max-height: 80vh;
            overflow-y: auto;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- 頂部導航 -->
    <nav class="bg-gradient-to-r from-red-500 to-red-600 text-white p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">🌅 退休福音</h1>
            <div class="text-sm" id="currentTime"></div>
        </div>
    </nav>

    <!-- PWA 安裝提示 -->
    <div class="pwa-hint bg-blue-100 text-blue-800 p-3 text-center text-sm">
        📱 Safari：點「分享」→「加入主畫面」可像 APP 使用
    </div>

    <div class="container mx-auto p-4">
        <!-- 統計卡片（可點擊） -->
        <div class="grid grid-cols-2 lg:grid-cols-5 gap-3 mb-6">
            <div class="stats-card bg-white rounded-xl shadow p-4 border-l-4 border-blue-500" onclick="showUsers()">
                <p class="text-gray-500 text-xs">用戶數</p>
                <p class="text-2xl font-bold text-gray-800"><%= stats.users %></p>
                <p class="text-lg">👥</p>
                <p class="text-xs text-blue-500 mt-1">點擊查看 →</p>
            </div>
            <div class="stats-card bg-white rounded-xl shadow p-4 border-l-4 border-green-500" onclick="showActivities()">
                <p class="text-gray-500 text-xs">活動數</p>
                <p class="text-2xl font-bold text-gray-800"><%= stats.activities %></p>
                <p class="text-lg">🎯</p>
                <p class="text-xs text-green-500 mt-1">點擊查看 →</p>
            </div>
            <div class="stats-card bg-white rounded-xl shadow p-4 border-l-4 border-yellow-500" onclick="showTours()">
                <p class="text-gray-500 text-xs">AI行程</p>
                <p class="text-2xl font-bold text-gray-800"><%= stats.tours %></p>
                <p class="text-lg">🌍</p>
                <p class="text-xs text-yellow-500 mt-1">點擊查看 →</p>
            </div>
            <div class="stats-card bg-white rounded-xl shadow p-4 border-l-4 border-purple-500" onclick="showReminders()">
                <p class="text-gray-500 text-xs">健康提醒</p>
                <p class="text-2xl font-bold text-gray-800"><%= stats.reminders %></p>
                <p class="text-lg">💊</p>
                <p class="text-xs text-purple-500 mt-1">點擊查看 →</p>
            </div>
            <div class="stats-card bg-white rounded-xl shadow p-4 border-l-4 border-pink-500" onclick="showWishlists()">
                <p class="text-gray-500 text-xs">想去收藏</p>
                <p class="text-2xl font-bold text-gray-800"><%= stats.wishlists %></p>
                <p class="text-lg">❤️</p>
                <p class="text-xs text-pink-500 mt-1">點擊查看 →</p>
            </div>
        </div>

        <!-- 快速操作 -->
        <div class="bg-white rounded-xl shadow p-4 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-3">⚡ 快速操作</h2>
            <div class="grid grid-cols-3 md:grid-cols-5 gap-2">
                <button onclick="refreshStats()" class="bg-blue-500 hover:bg-blue-600 text-white p-3 rounded-lg text-sm transition">
                    🔄 重整
                </button>
                <button onclick="syncDatabase()" class="bg-green-500 hover:bg-green-600 text-white p-3 rounded-lg text-sm transition">
                    🗄️ 同步
                </button>
                <button onclick="seedData()" class="bg-yellow-500 hover:bg-yellow-600 text-white p-3 rounded-lg text-sm transition">
                    📦 匯入
                </button>
                <a href="https://line.me/R/ti/p/@024wclps" target="_blank" class="bg-green-600 hover:bg-green-700 text-white p-3 rounded-lg text-sm text-center transition">
                    💬 LINE
                </a>
                <a href="/" class="bg-gray-500 hover:bg-gray-600 text-white p-3 rounded-lg text-sm text-center transition">
                    🏠 首頁
                </a>
            </div>
        </div>

        <!-- 功能狀態 -->
        <div class="bg-white rounded-xl shadow p-4 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-3">✅ 功能狀態</h2>
            <div class="grid grid-cols-2 gap-2 text-sm">
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>🌍 AI行程規劃</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>☁️ 天氣服務</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>💊 健康提醒</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>❤️ 想去收藏</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>⚙️ 個人設定</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
                <div class="flex items-center justify-between p-2 bg-green-50 rounded">
                    <span>⏰ 早安推播</span>
                    <span class="text-green-500 font-bold">✓</span>
                </div>
            </div>
        </div>

        <!-- 最近用戶 -->
        <div class="bg-white rounded-xl shadow p-4 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-3">👥 最近用戶</h2>
            <div class="overflow-x-auto">
                <table class="w-full text-sm">
                    <thead>
                        <tr class="border-b bg-gray-50">
                            <th class="text-left p-2">名稱</th>
                            <th class="text-left p-2">城市</th>
                            <th class="text-left p-2">加入時間</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% recentUsers.forEach(function(user) { %>
                        <tr class="border-b hover:bg-gray-50">
                            <td class="p-2"><%= user.displayName || '用戶' %></td>
                            <td class="p-2"><%= user.city || '未設定' %></td>
                            <td class="p-2"><%= new Date(user.createdAt).toLocaleDateString('zh-TW') %></td>
                        </tr>
                        <% }); %>
                        <% if (recentUsers.length === 0) { %>
                        <tr><td colspan="3" class="p-4 text-center text-gray-500">尚無用戶</td></tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- API 連結 -->
        <div class="bg-white rounded-xl shadow p-4 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-3">🔗 API 連結</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-2">
                <a href="/api/stats" target="_blank" class="bg-blue-100 hover:bg-blue-200 text-blue-800 p-3 rounded-lg text-sm text-center transition">
                    📊 統計
                </a>
                <a href="/api/activities" target="_blank" class="bg-green-100 hover:bg-green-200 text-green-800 p-3 rounded-lg text-sm text-center transition">
                    🎯 活動
                </a>
                <a href="/api/health" target="_blank" class="bg-purple-100 hover:bg-purple-200 text-purple-800 p-3 rounded-lg text-sm text-center transition">
                    ❤️ 健康檢查
                </a>
                <a href="/api/fix-db" target="_blank" class="bg-yellow-100 hover:bg-yellow-200 text-yellow-800 p-3 rounded-lg text-sm text-center transition">
                    🗄️ 同步DB
                </a>
            </div>
        </div>

        <!-- LINE Bot 資訊 -->
        <div class="bg-white rounded-xl shadow p-6 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-4 text-center">💬 LINE Bot</h2>
            <div class="flex flex-col items-center gap-4">
                <img src="https://qr-official.line.me/gs/M_024wclps_BW.png" alt="QR Code" class="w-40 h-40 border-4 border-green-500 rounded-lg shadow">
                <p class="text-gray-600 font-bold text-xl">@024wclps</p>
                <p class="text-gray-500 text-sm">掃描 QR Code 或搜尋 ID 加入好友</p>
                <div class="flex gap-3">
                    <a href="https://line.me/R/ti/p/@024wclps" target="_blank" class="bg-green-500 hover:bg-green-600 text-white px-6 py-3 rounded-lg font-bold transition">
                        💬 開啟 LINE
                    </a>
                    <button onclick="copyLink()" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg font-bold transition">
                        📋 複製連結
                    </button>
                </div>
            </div>
        </div>

        <!-- 操作日誌 -->
        <div class="bg-white rounded-xl shadow p-4 mb-6">
            <h2 class="text-lg font-bold text-gray-800 mb-3">📜 操作日誌</h2>
            <div id="logContainer" class="bg-gray-900 text-green-400 p-3 rounded-lg font-mono text-xs h-32 overflow-y-auto">
                <div>[系統] 管理後台已載入</div>
            </div>
        </div>
    </div>

    <!-- 底部導航 -->
    <footer class="bg-gray-800 text-white text-center p-4">
        <p class="text-sm">🌅 退休福音 © 2025</p>
        <p class="text-xs text-gray-400 mt-1">
            LINE Bot <a href="https://line.me/R/ti/p/@024wclps" target="_blank" class="text-green-400 hover:underline">@024wclps</a>
        </p>
    </footer>

    <!-- Modal 彈窗 -->
    <div id="modal" class="modal" onclick="closeModal(event)">
        <div class="modal-content" onclick="event.stopPropagation()">
            <div class="p-4 border-b flex justify-between items-center">
                <h3 id="modalTitle" class="text-lg font-bold">標題</h3>
                <button onclick="closeModal()" class="text-gray-500 hover:text-gray-800 text-2xl">&times;</button>
            </div>
            <div id="modalBody" class="p-4">
                載入中...
            </div>
        </div>
    </div>

    <script>
        // 更新時間
        function updateTime() {
            document.getElementById('currentTime').textContent = new Date().toLocaleString('zh-TW');
        }
        setInterval(updateTime, 1000);
        updateTime();

        // 日誌
        function addLog(msg) {
            var c = document.getElementById('logContainer');
            var d = document.createElement('div');
            d.textContent = '[' + new Date().toLocaleTimeString('zh-TW') + '] ' + msg;
            c.appendChild(d);
            c.scrollTop = c.scrollHeight;
        }

        // Modal 控制
        function openModal(title, content) {
            document.getElementById('modalTitle').textContent = title;
            document.getElementById('modalBody').innerHTML = content;
            document.getElementById('modal').classList.add('active');
        }

        function closeModal(e) {
            if (!e || e.target.id === 'modal') {
                document.getElementById('modal').classList.remove('active');
            }
        }

        // 顯示用戶列表
        async function showUsers() {
            addLog('載入用戶列表...');
            openModal('👥 用戶列表', '<p class="text-center text-gray-500">載入中...</p>');
            try {
                var r = await fetch('/api/admin/users');
                var d = await r.json();
                var html = '<div class="overflow-x-auto"><table class="w-full text-sm">';
                html += '<thead><tr class="border-b bg-gray-50"><th class="p-2 text-left">名稱</th><th class="p-2 text-left">城市</th><th class="p-2 text-left">推播</th><th class="p-2 text-left">加入</th></tr></thead><tbody>';
                if (d.data && d.data.length > 0) {
                    d.data.forEach(function(u) {
                        html += '<tr class="border-b"><td class="p-2">' + (u.displayName || '用戶') + '</td>';
                        html += '<td class="p-2">' + (u.city || '-') + '</td>';
                        html += '<td class="p-2">' + (u.notificationEnabled ? '✅' : '❌') + '</td>';
                        html += '<td class="p-2">' + new Date(u.createdAt).toLocaleDateString('zh-TW') + '</td></tr>';
                    });
                } else {
                    html += '<tr><td colspan="4" class="p-4 text-center text-gray-500">尚無用戶</td></tr>';
                }
                html += '</tbody></table></div>';
                openModal('👥 用戶列表 (' + (d.data ? d.data.length : 0) + ')', html);
                addLog('用戶列表載入完成');
            } catch (e) {
                openModal('👥 用戶列表', '<p class="text-red-500">載入失敗：' + e.message + '</p>');
                addLog('錯誤: ' + e.message);
            }
        }

        // 顯示活動列表
        async function showActivities() {
            addLog('載入活動列表...');
            openModal('🎯 活動列表', '<p class="text-center text-gray-500">載入中...</p>');
            try {
                var r = await fetch('/api/activities?limit=50');
                var d = await r.json();
                var html = '<div class="space-y-2">';
                if (d.data && d.data.length > 0) {
                    d.data.forEach(function(a) {
                        html += '<div class="p-3 bg-gray-50 rounded-lg">';
                        html += '<p class="font-bold">' + a.name + '</p>';
                        html += '<p class="text-sm text-gray-500">📍 ' + (a.city || '') + ' | ⭐ ' + (a.rating || 4.5) + '</p>';
                        html += '</div>';
                    });
                } else {
                    html += '<p class="text-center text-gray-500">尚無活動</p>';
                }
                html += '</div>';
                openModal('🎯 活動列表 (' + (d.total || 0) + ')', html);
                addLog('活動列表載入完成');
            } catch (e) {
                openModal('🎯 活動列表', '<p class="text-red-500">載入失敗：' + e.message + '</p>');
                addLog('錯誤: ' + e.message);
            }
        }

        // 顯示行程列表
        async function showTours() {
            addLog('載入AI行程...');
            openModal('🌍 AI行程', '<p class="text-center text-gray-500">載入中...</p>');
            try {
                var r = await fetch('/api/admin/tours');
                var d = await r.json();
                var html = '<div class="space-y-2">';
                if (d.data && d.data.length > 0) {
                    d.data.forEach(function(t) {
                        html += '<div class="p-3 bg-gray-50 rounded-lg">';
                        html += '<p class="font-bold">' + t.name + '</p>';
                        html += '<p class="text-sm text-gray-500">📍 ' + (t.country || '') + ' | 📅 ' + (t.days || 5) + '天</p>';
                        html += '</div>';
                    });
                } else {
                    html += '<p class="text-center text-gray-500">尚無行程</p>';
                }
                html += '</div>';
                openModal('🌍 AI行程 (' + (d.data ? d.data.length : 0) + ')', html);
                addLog('AI行程載入完成');
            } catch (e) {
                openModal('🌍 AI行程', '<p class="text-red-500">載入失敗：' + e.message + '</p>');
                addLog('錯誤: ' + e.message);
            }
        }

        // 顯示健康提醒
        async function showReminders() {
            addLog('載入健康提醒...');
            openModal('💊 健康提醒', '<p class="text-center text-gray-500">載入中...</p>');
            try {
                var r = await fetch('/api/admin/reminders');
                var d = await r.json();
                var html = '<div class="space-y-2">';
                if (d.data && d.data.length > 0) {
                    d.data.forEach(function(h) {
                        var icon = h.type === 'appointment' ? '🏥' : '💊';
                        html += '<div class="p-3 bg-gray-50 rounded-lg">';
                        html += '<p class="font-bold">' + icon + ' ' + h.title + '</p>';
                        if (h.type === 'appointment') {
                            html += '<p class="text-sm text-gray-500">' + (h.hospitalName || '') + ' | ' + (h.appointmentDate || '') + '</p>';
                        } else {
                            html += '<p class="text-sm text-gray-500">' + (h.medicationName || '') + '</p>';
                        }
                        html += '</div>';
                    });
                } else {
                    html += '<p class="text-center text-gray-500">尚無提醒</p>';
                }
                html += '</div>';
                openModal('💊 健康提醒 (' + (d.data ? d.data.length : 0) + ')', html);
                addLog('健康提醒載入完成');
            } catch (e) {
                openModal('💊 健康提醒', '<p class="text-red-500">載入失敗：' + e.message + '</p>');
                addLog('錯誤: ' + e.message);
            }
        }

        // 顯示想去收藏
        async function showWishlists() {
            addLog('載入想去收藏...');
            openModal('❤️ 想去收藏', '<p class="text-center text-gray-500">載入中...</p>');
            try {
                var r = await fetch('/api/admin/wishlists');
                var d = await r.json();
                var html = '<div class="space-y-2">';
                if (d.data && d.data.length > 0) {
                    d.data.forEach(function(w) {
                        html += '<div class="p-3 bg-gray-50 rounded-lg">';
                        html += '<p class="font-bold">' + (w.Activity ? w.Activity.name : '活動') + '</p>';
                        html += '<p class="text-sm text-gray-500">' + (w.isVisited ? '✅ 已去過' : '📍 想去') + '</p>';
                        html += '</div>';
                    });
                } else {
                    html += '<p class="text-center text-gray-500">尚無收藏</p>';
                }
                html += '</div>';
                openModal('❤️ 想去收藏 (' + (d.data ? d.data.length : 0) + ')', html);
                addLog('想去收藏載入完成');
            } catch (e) {
                openModal('❤️ 想去收藏', '<p class="text-red-500">載入失敗：' + e.message + '</p>');
                addLog('錯誤: ' + e.message);
            }
        }

        // 重新整理
        function refreshStats() {
            addLog('正在重新整理...');
            location.reload();
        }

        // 同步資料庫
        async function syncDatabase() {
            addLog('正在同步資料庫...');
            try {
                var r = await fetch('/api/fix-db');
                var d = await r.json();
                addLog(d.message || '同步完成');
            } catch (e) {
                addLog('錯誤: ' + e.message);
            }
        }

        // 匯入種子資料
        async function seedData() {
            if (!confirm('確定要重新匯入種子資料？')) return;
            addLog('正在匯入種子資料...');
            try {
                var r = await fetch('/api/seed?force=true');
                var d = await r.json();
                addLog(d.message || '匯入完成');
            } catch (e) {
                addLog('錯誤: ' + e.message);
            }
        }

        // 複製連結
        function copyLink() {
            navigator.clipboard.writeText('https://line.me/R/ti/p/@024wclps').then(function() {
                addLog('連結已複製');
                alert('連結已複製！');
            });
        }

        // PWA 檢測
        if (window.matchMedia('(display-mode: standalone)').matches) {
            var hint = document.querySelector('.pwa-hint');
            if (hint) hint.style.display = 'none';
        }

        addLog('統計資料已載入');
    </script>
</body>
</html>